module.exports = {
  plugins: [
    require('tailwindcss'),
    require('autoprefixer'),
    plugin(function({addComponents, theme})) {
      const inputs ; {
        '.text-input': {
          width: '100%',
        }
      }
    }
  ]
}

// TODO: something down here.
