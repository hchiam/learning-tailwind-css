{
  "name": "learning-tailwind-css",
  "version": "1.0.0",
  "private": true,
  "description": "A CSS utility framework. Don't look like a standard Bootstrap site. Configurable with a JS config file. Extendible with custom classes.",
  "main": "src/index.js",
  "scripts": {
    "start": "rm -rf dist || true; parcel src/index.html --out-dir dist --cache-dir .cache --open;"
  },
  "author": "hchiam",
  "license": "MIT",
  "dependencies": {
    "autoprefixer": "^9.7.4",
    "postcss-cli": "^7.1.0",
    "tailwindcss": "^1.2.0"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4",
    "precss": "^4.0.0"
  }
}
